# {{ project_name }} Agent Operating Guide

Generated by Frontier Harness Template `{{ template_version }}` using profile `{{ profile_name }}`.

## Project Boundary

This harness belongs to `{{ project_name }}` only. Agents must keep edits inside this repository unless a human explicitly authorizes another path.

## Profile

- Description: {{ profile.description }}
- Default branch: `{{ profile.default_branch }}`
- Primary language: `{{ profile.primary_language }}`
- Automation lane: `{{ profile.automation_lane }}`
- Risk level: `{{ profile.risk_level }}`
- Human review required: `{{ profile.requires_human_review }}`
- Trading enabled: `{{ profile.trading_enabled }}`
- Broker enabled: `{{ profile.broker_enabled }}`

## Operating Rules

- Read project context before editing.
- Keep changes small, reviewable, and tied to an active campaign or spec.
- Run the relevant validation before handoff.
- Record decisions in `decisions/` when a durable architectural choice is made.
- Record handoffs in `handoffs/` when work is paused, transferred, or ready for review.
- Do not auto-merge or deploy without explicit human approval.

## Required Artifacts

- Campaign plans live in `campaigns/`.
- Specs live in `specs/`.
- Reviews live in `reviews/`.
- Run logs live in `runs/`.
- Project docs live in `docs/`.
