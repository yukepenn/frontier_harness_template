# Ralph Scripts

Run a scaffolded Workflow 2 campaign:

```bash
scripts/ralph/ralph.sh --campaign-id CAMPAIGN_ID
```

Resume a scaffolded run:

```bash
scripts/ralph/ralph.sh resume --run-id RUN_ID
```

This is a strict-loop scaffold, not a production autonomous driver.
