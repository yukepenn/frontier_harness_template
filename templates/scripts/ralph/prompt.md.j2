# Ralph Prompt Scaffold

Project: `{{ project_name }}`

Use the Frontier Harness strict state machine. Read `AGENTS.md`, `frontier.yaml`, `ACTIVE_CAMPAIGN.md`, the active campaign, and the current run state before taking action.

Do not chat casually. Advance the next state, write durable artifacts, and stop when policy requires a block.
