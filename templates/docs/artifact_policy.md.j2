# Artifact Policy

Allowed by default:

- harness files
- source files
- tests
- docs
- specs, handoffs, reviews, decisions
- curated evals and configs
- small markdown, JSON, or CSV summaries where policy permits

Forbidden by default:

- `.env` and `.env.*`
- key, PEM, token, credential, and private-key-like files
- raw data directories
- caches
- local virtual environments
- `node_modules`
- large binary artifacts
- model weights
- generated logs
- parquet, sqlite, DuckDB, pickle/joblib, ONNX, pt, and pth files

The authoritative policy lives in `frontier.yaml` and is enforced by `tools/hooks/secret_scan.py`, `tools/hooks/artifact_guard.py`, and generated canaries.
