name = "worker"
description = "Primary implementation and repair agent for Frontier phases."
model = "gpt-5.5"
effort = "high"
default_skill = "frontier-execute"
requires_human_review = {{ profile.requires_human_review }}
